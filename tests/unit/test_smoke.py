"""Phase 0 smoke test — proves the pytest harness itself works.

Replace/supplement with real lib/schema and lib/normalize tests starting phase 1.
"""


def test_harness_runs():
    assert 1 + 1 == 2
